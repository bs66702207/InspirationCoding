count-substr-in-mainstr-number.c 
计算字符串中字串的个数, 思路: 利用strstr循环进行查找， 并且统计个数。

insert-continuous-num.c 
找找字符串中连续字母的个数， 并且将个数分别插入到连续字母的后面, AABCC -> AA2BCC2, 思路: 分两步走，第一步是一个循环中打印出字母连续的数字; 第二步是在这个循环中，截取出连续字母/独立字母的字符串，如果是连续字母字符串，用strcat接上数字，如果是独立的字母就不接上数字，然后从头到尾巴拼接起来。
